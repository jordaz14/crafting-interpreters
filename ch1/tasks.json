{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "command": "clang",
      "args": [
        "-g",
        "${file}",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$clang"],
      "detail": "Build task"
    }
  ]
}
